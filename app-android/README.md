// CMP Android app — OWNER: Agent B.
// Collector: UsageStatsManager poll + persist. No accessibility service:
// UsageStats is the authoritative source.
// UI: ported Compose screens. Hardening: allowBackup=false, keystore.
//
// Read the Two-Agent Execution Contract in docs/plan.md before touching
// this directory. Agent A: do not modify files here.
